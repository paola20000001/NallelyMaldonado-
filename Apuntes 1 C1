CLASE 1 
CUENTA  DE AHORROS 
INGRESE SALDO INICIAL =$
500
INGRESE TASA DE INTERES ANUAL=10
SELECCIONE UNA OPCION 

1 DEPOSITAR
RETIRAR
        VER SALDO
                SALIR 

SALDO =

SON 2 CLASES 
1 PARA MANEJAR LA CUENTA
1 PARA MANEJAR EL MENU
MAIN QUE CONTROLA LO QUE INGRESA Y LA TASA Y REDIRIGE A LAS OTRAS CLASES


//////////////////////////////////////////////////////////////

public class Cuenta {
    private double saldo;
    private double tasaInteres;

    public Cuenta(double saldoInicial, double tasaInteresAnual) {
        this.saldo = saldoInicial;
        this.tasaInteres = tasaInteresAnual;
    }

    public void depositar(double monto) {
        saldo += monto;
    }

    public boolean retirar(double monto) {
        if (monto <= saldo) {
            saldo -= monto;
            return true;
        } else {
            return false;
        }
    }

    public double getSaldo() {
        return saldo;
    }

    public double getTasaInteres() {
        return tasaInteres;
    }
}
